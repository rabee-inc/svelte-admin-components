import { headings, schemas, sections } from "./template";

export default {
  label: "user",
  headings: [
    headings.id,
  ],
  sections: [
    {
      label: "メイン",
      class: "col12",
      schemas: [
        schemas.id,
      ]
    }
  ]
};
